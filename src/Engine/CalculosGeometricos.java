package Engine;

import java.awt.geom.Point2D;

public class CalculosGeometricos {
    public static double produtoEscalar(double vx, double vy, double nx, double ny) {
        return (vx * nx) + (vy * ny);
    }
}
