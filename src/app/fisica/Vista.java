package app.fisica;

/** Como o ensaio deve ser desenhado. */
public enum Vista {
    /** Visto de cima: x na horizontal, y na vertical. */
    PLANO,
    /** Visto de lado: x na horizontal, altura na vertical. */
    PERFIL
}
